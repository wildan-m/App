type UpdateBusinessCentralEnableNewCategoriesParams = {
    /** The workspace where the setting is updated. */
    policyID: string;

    /** Whether categories newly created in Business Central are imported. */
    enabled: boolean;
};

export default UpdateBusinessCentralEnableNewCategoriesParams;
