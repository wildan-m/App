/** Inner padding between bars (0.3 = 30% of bar width) */
const BAR_INNER_PADDING = 0.3;

/** Vertical space reserved for each bar row in the horizontal bar chart, in pixels */
const MIN_BAR_ROW_HEIGHT = 32;

/** Fraction of the chart width the category label column may occupy in the horizontal bar chart */
const CATEGORY_LABEL_MAX_WIDTH_RATIO = 0.4;

export default BAR_INNER_PADDING;
export {MIN_BAR_ROW_HEIGHT, CATEGORY_LABEL_MAX_WIDTH_RATIO};
