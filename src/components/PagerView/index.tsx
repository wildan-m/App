import React from 'react';
import { View } from 'react-native';

// eslint-disable-next-line @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any
function PagerView(props: any) {
    return <View {...props} />;
}

export default PagerView;